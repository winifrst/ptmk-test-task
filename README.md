# ptmk-test-task

зависимости Установите библиотеку sqlite3
Если sqlite3 не установлен, установите его:

На Ubuntu/Debian:
bash Copysudo apt-get install libsqlite3-dev

На Fedora/RHEL:
bash Copysudo dnf install sqlite-devel

На Arch Linux:
bash Copysudo pacman -S sqlite

